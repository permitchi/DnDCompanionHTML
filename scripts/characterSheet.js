let str;
let dex;
let con;
let int;
let wis;
let cha;

fetch('data/data.json')
    // .then(response => response.json())
    // .then(data => {
    //     attributeData = data;
    // })
    // .catch(error => console.error('Error loading data:', error));

function countModifier(attribute) {
    for (let i = 0; i <= data.attributes.length; i++) {
        console.log("hi");
    }
}